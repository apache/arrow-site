# Open a memory mapped file

Open a memory mapped file

## Usage

``` r
mmap_open(path, mode = c("read", "write", "readwrite"))
```

## Arguments

- path:

  file path

- mode:

  file mode (read/write/readwrite)
